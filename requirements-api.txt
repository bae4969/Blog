# 블로그 앱 이미지 전용 추가 의존성.
#
# ⚠️ 공용 베이스 이미지(fastapi-py312)에 없는 것만 적는다. Docker 이미지 빌드 때 설치하며
#    컨테이너 기동 때는 pip를 실행하지 않는다.
#
# nh3 — HTML 정화(Rust ammonia 바인딩). **본문 출력의 유일한 방어선**이다:
#   PHP 는 저장할 때 정화하지 않고 화면에 뿌릴 때만 HTMLPurifier 를 태운다. 즉 DB 에는
#   원본이 그대로 들어 있어, 정화를 빼먹으면 그대로 XSS 가 된다.
nh3==0.2.18
