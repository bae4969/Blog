.vscode/
.claude/
CLAUDE.md
temp/
cache/
vendor/
config/
# 사용자가 올린 이미지와 글 썸네일. 배포로 따라가지 않고 환경마다 각자 갖는다.
public/uploads/

# FastAPI 포팅본 — 환경 설정(계정·비밀번호). .env 와 같은 이유로 제외한다.
.env.api

# 파이썬 바이트코드 — 컨테이너가 실행하며 만든다. 커밋에 딸려 들어간 적 있다.
__pycache__/
*.pyc
