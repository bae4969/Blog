"""외부에 내주는 API. 화면(`app/ui`)과 **같은 데이터를 같은 규칙으로** 본다.

## 화면용 AJAX(`/stocks/api/*`)와 무엇이 다른가

그쪽은 `require_internal`(`X-Requested-With` + Origin/Referer)을 요구해서 **브라우저
전용**이다. 앱이나 스크립트는 원리적으로 못 붙는다 — 이름만 API 지 사실상 내 화면의
창구다. 여기는 그 검사를 걸지 않고 **토큰(쿠키 또는 `Authorization: Bearer`)** 으로만
누구인지 판단한다.

## 1단계는 읽기(GET)만이다

쓰기를 열려면 CSRF 를 다시 설계해야 한다. 쿠키로 인증되는 요청은 브라우저가 자동으로
쿠키를 실어 보내므로 교차 출처 쓰기를 막을 장치가 필요하고, Bearer 만 허용할지 둘 다
받을지는 소비자가 정해진 뒤에 정하는 편이 낫다. 지금 열어 두면 되돌리기 어렵다.

## 응답 규약

- 성공은 **리소스를 그대로** 돌려준다. `{success: true, data: ...}` 같은 봉투를 씌우지
  않는다 — 목록만 `Page` 로 감싼다(전체 건수·쪽수를 실어야 해서다).
- 실패는 **HTTP 상태코드**로 말한다(404·403·422). 옛 `/stocks/api/*` 는 200 에
  `{success: false}` 를 담는데, 그러면 소비자가 매번 본문을 열어봐야 한다.
"""
