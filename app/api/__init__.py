"""외부에 내주는 API. 화면(`app/ui`)과 **같은 데이터를 같은 규칙으로** 본다.

## 화면용 AJAX(`/stocks/api/*`)를 대신한다 — 그쪽은 이제 없다

옛 층은 `require_internal`(`X-Requested-With` + Origin/Referer)을 요구해서 **브라우저
전용**이었다. 앱이나 스크립트는 원리적으로 못 붙었다 — 이름만 API 지 사실상 내 화면의
창구였다. 2026-08-19 에 12개를 전부 여기로 옮기고 지웠다.

여기는 그 검사를 걸지 않고 **토큰(쿠키 또는 `Authorization: Bearer`)** 으로만 누구인지
판단한다.

## 쓰기는 Bearer 전용이다

쿠키는 브라우저가 요청마다 자동으로 붙여 교차 출처 쓰기가 성립하지만, `Authorization`
헤더는 남의 사이트가 붙일 수 없다. 그래서 쓰기는 Bearer 만 받는다(`deps.py`).

화면도 그 규칙을 그대로 지킨다 — 인증 쿠키가 `httponly` 라 JS 가 토큰을 못 읽으므로
`POST /api/v1/auth/token` 으로 지금 세션의 토큰을 받아 헤더에 싣는다(`auth_v1.py`).

## 응답 규약

- 성공은 **리소스를 그대로** 돌려준다. `{success: true, data: ...}` 같은 봉투를 씌우지
  않는다 — 목록만 `Page` 로 감싼다(전체 건수·쪽수를 실어야 해서다).
- 실패는 **HTTP 상태코드**로 말한다(404·403·422). 옛 `/stocks/api/*` 는 200 에
  `{success: false}` 를 담았는데, 그러면 소비자가 매번 본문을 열어봐야 한다.
"""
